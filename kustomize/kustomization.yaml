resources:
- app.yaml

patches:
- target:
    group: apps
    version: v1
    kind: Deployment
    name: .*
  path: patch-apikeys-env.yaml
- target:
    group: apps
    version: v1
    kind: Deployment
    name: .*
  path: patch-stigg-sidecar.yaml
