## @section Common parameters
# You can provide your own values when installing the chart.
# You can override these values in your own Helm chart's values.yaml file. For example:
#
# stiggchart:
#   serverApiKey: "your-server-api-key"

## @param serverApiKey Stigg server API key to store in a new created secret
serverApiKey: "<SERVER_API_KEY>"
## @param apiKeysSecretName Use an existing secret with keys "stigg_server_api_key" defined.
## This ignores serverApiKey
apiKeysSecretName: ""
## @param sidecarImageTag Docker image tag for the Stigg Sidecar
sidecarImageTag: "latest"
## @param persistentCacheImageTag Docker image tag for the Stigg Persistent Cache
persistentCacheImageTag: "2.39.0"
## @param persistentCache Enable persistent cache (true/false)
persistentCache: false
## @param redisEnvironmentPrefix Prefix for Redis environment keys
redisEnvironmentPrefix: ""
## @param redisHost Hostname for Redis instance
redisHost: ""
## @param redisPort Redis port (default: 6379)
redisPort: "6379"
## @param redisDb Redis DB identifier (default: 0)
redisDb: "0"
## @param redisUsername Redis username (optional)
redisUsername: ""
## @param redisPassword Redis password (optional)
redisPassword: "testpassword"
## @param redisTls Enable TLS for Redis connection (true/false)
redisTls: true
## @param awsRegion AWS region for SQS queue, provisioned by Stigg
awsRegion: ""
## @param queueUrl URL of AWS SQS queue, provisioned by Stigg
queueUrl: "<QUEUE_URL>"
## @param persistentCacheResources Resource requests and limits for persistent cache service
persistentCacheResources:
  cpu:
    request: "1000m"
    limit: "1500m"
  memory:
    request: "512Mi"
    limit: "640Mi"
## @param persistentCacheHpa Horizontal Pod Autoscaler configuration for persistent cache service
persistentCacheHpa:
  minReplicas: 1
  maxReplicas: 4
  targetCPUUtilizationPercentage: 70
  targetMemoryUtilizationPercentage: 80
## @param extraDeploy Array of extra objects to deploy with the release
extraDeploy: []
